import * as alt from 'alt-client';

export const visualTextLabelConfig = {
    fontName: "default",
    fontSize: 100,
    scale: 1,
    color: new alt.RGBA(alt.RGBA.white),
    outlineWidth: 2,
    outlineColor: new alt.RGBA(alt.RGBA.black),
    useStreaming: true,
    streamingDistance: 100
}